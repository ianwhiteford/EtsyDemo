<% @posts.each do |post| %>
	<h2><%= link_to post.title, post %></h2>
	<p><%= post.created_at.strftime("%B, %d, %Y") %> </p>
<% end %>

<%= link_to 'New', new_post_path(@post), class: "btn btn-link" %>